{
  "name": "badgee",
  "author": "Olivier Audard <audard@gmail.com>",
  "version": "1.2.3",
  "description": "a browser console improvement",
  "main": "build/badgee.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:dharFr/badgee.git"
  },
  "license": "MIT",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "npm run dev",
    "pretest": "eslint src test && npm run build",
    "test": "mocha -R spec --require jsdom-global/register --compilers js:buble/register test/",
    "prebuild": "npm run clean",
    "build": "rollup -c rollup.config.js -f umd --name $npm_package_name --output $npm_package_main",
    "watch": "watch 'npm run build' src",
    "postbuild": "npm run minify",
    "clean": "rm -rf build/*",
    "minify": "uglifyjs build/badgee.js > build/badgee.min.js",
    "preversion": "npm run test",
    "version": "npm run build && git add -A build",
    "postversion": "git push --tags origin master",
    "serve": "http-server -p 8000 .",
    "open": "opener http://localhost:8000/test/",
    "live-reload": "live-reload --port 8001 build/",
    "dev": "npm run open -s & parallelshell 'npm run live-reload -s' 'npm run serve -s' 'npm run watch -s'"
  },
  "devDependencies": {
    "buble": "^0.15.2",
    "chai": "^3.4.1",
    "eslint": "^3.19.0",
    "http-server": "^0.9.0",
    "jsdom": "^9.12.0",
    "jsdom-global": "^2.1.1",
    "live-reload": "^1.1.0",
    "mocha": "^3.0.2",
    "opener": "^1.4.1",
    "parallelshell": "^2.0.0",
    "rollup": "^0.41.6",
    "rollup-plugin-buble": "^0.15.0",
    "sinon": "^2.0.0",
    "uglify-js": "^2.6.1",
    "watch": "^1.0.2"
  },
  "dependencies": {}
}
